set -g mouse off
set -g mode-keys vi
set -s escape-time 0

# WSL2/터미널 capability 쿼리 응답이 프롬프트에 섞이지 않도록
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# WSL2: DA2 쿼리 응답(61;4;...c)이 셸로 새는 현상 방지
# Windows Terminal 응답이 셸 stdin으로 흘러드는 타이밍 문제 — WSL2 전용
if-shell "grep -qi microsoft /proc/version 2>/dev/null" \
  "set -g focus-events off" \
  "set -g focus-events on"

# Mouse toggle: prefix + m / prefix + M
bind m set -g mouse on \; display-message "mouse on"
bind M set -g mouse off \; display-message "mouse off"

bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi Enter send-keys -X copy-selection-and-cancel

# 클립보드: OS별 자동 감지
if-shell "grep -qi microsoft /proc/version 2>/dev/null" \
  "bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'clip.exe'" \
  "if-shell 'uname | grep -q Darwin' \
    'bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel pbcopy' \
    'bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel wl-copy'"

# prefix 키를 C-a 로 변경
set -g prefix 'C-a'
set -g status-left "[#S] "
set -g status-left-length 30
set -g status-right "#H"
set -g status-right-length 30
set -g base-index 1
set -g pane-base-index 1
unbind C-b

# 활성 pane 테두리 강조
set -g pane-active-border-style fg=#a6e3a1
set -g pane-border-style fg=#45475a

# 직관적인 분할 단축키
bind "|" split-window -h -c "#{pane_current_path}"   # 세로 분할 (| 모양)
bind "-" split-window -v -c "#{pane_current_path}"   # 가로 분할 (- 모양)
unbind '"'
unbind %

# 창 간 이동: Alt + 화살표 (prefix 없이)
bind -r Left  select-pane -L
bind -r Right select-pane -R
bind -r Up    select-pane -U
bind -r Down  select-pane -D

# 창 크기 조절: prefix + Shift + 화살표
bind -r S-Left  resize-pane -L 5
bind -r S-Right resize-pane -R 5
bind -r S-Up    resize-pane -U 5
bind -r S-Down  resize-pane -D 5

# 새 탭: prefix + c (기본값 유지), 탭 이름 변경: prefix + ,
# 탭 닫기: prefix + Q  /  pane 닫기: prefix + x
unbind &
bind x confirm-before -p "kill pane #P? (y/n)" kill-pane
bind Q confirm-before -p "kill window #S:#W? (y/n)" kill-window

# 설정 리로드: prefix + r
bind r source-file ~/.tmux.conf \; display "tmux.conf reloaded!"

# 마우스 토글: prefix + m (ON) / prefix + M (OFF)
bind m set -g mouse on \; display "Mouse ON"
bind M set -g mouse off \; display "Mouse OFF"

# nested tmux: send local prefix to remote
bind-key -n C-b send-prefix  # prefix + C-a 로 현재 prefix(C-a)를 원격 tmux로 전달

# macOS: Solarized Dark / 그 외(Linux/WSL): Solarized Light
if-shell 'uname | grep -q Darwin' \
  'set -g status-style                bg=#002b36,fg=#839496; \
   set -g status-left-style           bg=#002b36,fg=#586e75; \
   set -g status-right-style          bg=#002b36,fg=#586e75; \
   set -g window-status-style         bg=#002b36,fg=#657b83; \
   set -g window-status-current-style bg=#002b36,fg=#2aa198,bold; \
   set -g pane-border-style           fg=#073642; \
   set -g pane-active-border-style    fg=#cb4b16; \
   set -g message-style               bg=#073642,fg=#cb4b16' \
  'set -g status-style                bg=#eee8d5,fg=#657b83; \
   set -g status-left-style           bg=#eee8d5,fg=#586e75; \
   set -g status-right-style          bg=#eee8d5,fg=#586e75; \
   set -g window-status-style         bg=#eee8d5,fg=#839496; \
   set -g window-status-current-style bg=#eee8d5,fg=#268bd2,bold; \
   set -g pane-border-style           fg=#eee8d5; \
   set -g pane-active-border-style    fg=#cb4b16; \
   set -g message-style               bg=#eee8d5,fg=#cb4b16'
